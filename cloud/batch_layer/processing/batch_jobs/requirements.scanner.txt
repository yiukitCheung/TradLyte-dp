# Scanner Batch Job — lean dependency set
# Used by Dockerfile.scanner (separate from the resampler/consolidator image).
# No DuckDB, pandas, or pyarrow — the scanner only uses Polars + SQLAlchemy.

# OHLCV loading and in-memory resampling (inputs.py)
polars>=0.20.0

# Pydantic — SignalResult and strategy models
pydantic>=2.0.0

# PostgreSQL driver — rds_timescale_client.py uses psycopg2 directly
psycopg2-binary>=2.9.9

# AWS SDK — Secrets Manager + S3 (chunk file download)
boto3>=1.34.0

# Date helpers
python-dateutil>=2.8.0
pytz>=2023.3
