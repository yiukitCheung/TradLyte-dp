# scanner Lambda dependencies (dev-batch-scanner)
# boto3 is pre-installed in the Lambda runtime — not listed here.
# polars          → full-universe scan (.over(symbol)) + native Parquet read
# psycopg2-binary → write signals to daily_scan_signals; also imported at
#                   analytics_core.inputs load time
# pydantic        → analytics_core.models (StepConfig/SignalResult) imported by
#                   the bundled strategy library
polars>=0.20.0
psycopg2-binary>=2.9.9
pydantic>=2.0.0
