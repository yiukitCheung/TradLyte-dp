# daily_ohlcv_ingest_handler + daily-ohlcv-planner (requirements file path only; planner built in fetching/deploy_lambda.sh)
# psycopg2-binary must be installed with Linux manylinux wheels (see infrastructure/common/pip_for_lambda.sh).
# Lambda runtime Python version must match LAMBDA_PYTHON_VERSION when deploying (default 3.11).
boto3>=1.28.0
pydantic>=2.0.0,<3.0.0
psycopg2-binary>=2.9.0
pyarrow>=14.0.0
pytz>=2023.3
